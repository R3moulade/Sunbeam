var cars = document.getElementById("booking-rows");
    cars.style.display = "none";

    function showCars(post) {
        const minivan = document.getElementById("minivan");
        const standard = document.getElementById("standard");
        const budget = document.getElementById("budget");
    
        for (const car of cars) {
            if (toilet.properties.status === "Out of order") {
            outputoutoforder.insertAdjacentHTML("beforeend", `${toilet.properties.status}: ${toilet.properties.kommune + " " + toilet.properties.postnr_ + ", " + toilet.properties.adresse}<br>`)
        } else {
                outputactive.insertAdjacentHTML("beforeend", `${toilet.properties.status}: ${toilet.properties.kommune + " " + toilet.properties.postnr_ + ", " + toilet.properties.adresse}<br>`)
        }
    }
    }